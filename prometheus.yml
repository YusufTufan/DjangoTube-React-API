global:
  scrape_interval: 15s # Her 15 saniyede bir verileri çek

scrape_configs:
  - job_name: 'django'
    scrape_interval: 5s
    static_configs:
      - targets: ['backend:8000'] # Docker içindeki backend servisimizin adı
    metrics_path: /prometheus/metrics
    